[tool.black]
line-length = 100
target-version = ["py39"]

[tool.flake8]
max-line-length = 100
extend-ignore = ["E203", "W503", "F401", "F811"]
exclude = ".git,__pycache__,.venv,tests"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"